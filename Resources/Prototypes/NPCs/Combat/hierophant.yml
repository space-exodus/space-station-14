- type: htnCompound
  id: AbilityFightCompound
  branches:
    - tasks:
        - !type:HTNCompoundTask
          task: AbilityCombatCompound
    - tasks:
        - !type:HTNCompoundTask
          task: IdleCompound

- type: htnCompound
  id: AbilityCombatCompound
  branches:
    - preconditions:
      - !type:BuckledPrecondition
        isBuckled: true
      tasks:
      - !type:HTNPrimitiveTask
        operator: !type:UnbuckleOperator
          shutdownState: TaskFinished

    # Exodus-MRP NPC-Start
    - preconditions:
      - !type:StandingPrecondition
        isStanding: false
      tasks:
      - !type:HTNPrimitiveTask
        operator: !type:StandUpOperator
          shutdownState: TaskFinished
    # Exodus-MRP NPC-End

    - preconditions:
      - !type:InContainerPrecondition
        isInContainer: true
      tasks:
      - !type:HTNCompoundTask
        task: EscapeCompound

    - preconditions:
      - !type:PulledPrecondition
        isPulled: true
        isHostilePulling: true # Exodus-MRP NPC
      tasks:
      - !type:HTNPrimitiveTask
        operator: !type:UnPullOperator
          shutdownState: TaskFinished

    # Melee combat (unarmed or otherwise)
    - tasks:
        - !type:HTNCompoundTask
          task: AbilityAttackTargetCompound

# Tries to shoot a target at range.
- type: htnCompound
  id: AbilityAttackTargetCompound
  branches:
    # Pick target, then move into range and shoot them.
    - tasks:
        - !type:HTNPrimitiveTask
          operator: !type:UtilityOperator
            proto: NearbyGunTargets

        - !type:HTNPrimitiveTask
          operator: !type:MoveToOperator
            shutdownState: PlanFinished
            pathfindInPlanning: true
            removeKeyOnFinish: false
            targetKey: TargetCoordinates
            pathfindKey: TargetPathfind
            rangeKey: MeleeRange

        - !type:HTNPrimitiveTask
          preconditions:
            - !type:KeyExistsPrecondition
              key: Target
          operator: !type:AbilityOperator
            actions:
              - actionId: ActionSpawnDEVSpell
                maxRange: 4
                minRange: 0
            targetKey: Target
          services:
            - !type:UtilityService
              id: RangedService
              proto: NearbyGunTargets
              key: Target
