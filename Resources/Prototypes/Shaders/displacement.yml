- type: shader
  id: DisplacedStencilDraw
  kind: source
  path: "/Textures/Shaders/displacement.swsl"
  # Exodus-Naked-Fix-Start
  # stencil:
  #   ref: 1
  #   op: Keep
  #   func: NotEqual
  # Exodus-Naked-Fix-End
  params:
    displacementSize: 127
